import { StyleSheet } from "react-native"

export const estilos = StyleSheet.create({
    container: {
        backgroundColor: "#fff",
        height: 100, 
        width: 100,
        borderRadius: 10,
        margin: 10,
        justifyContent: 'center',
        alignItems: 'center'
    },

    imagem: {
        backgroundColor: "#fff",
        height: 90, 
        width: 90,
        borderRadius: 10,
    }
})
