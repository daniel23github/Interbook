import { StyleSheet } from "react-native"

export const estilos = StyleSheet.create({
    container: {
        backgroundColor: "#38B6FF",
        borderRadius: 10,
        textAlign: "center",
        margin: 10,
        padding: 10,
        paddingBottom: 20
    }
})
